package com.example.healthcareplus.theme

import androidx.compose.ui.graphics.Color

// ─── Primary Brand ───────────────────────────────────────────────────────────
val Primary            = Color(0xFF2D4ED8)   // Main blue (header/banner bg)
val PrimaryDark        = Color(0xFF1E3DB8)   // Darker blue (gradient bottom)
val PrimaryLight       = Color(0xFF4F6EF7)   // Lighter blue (highlights)
val PrimaryContainer   = Color(0xFFE6EDFF)   // Blue tint background

// ─── Secondary / Accent ──────────────────────────────────────────────────────
val Secondary          = Color(0xFF3ECFAA)   // Teal/mint (Doctor icon bg, accents)
val SecondaryContainer = Color(0xFFD1F5ED)   // Teal tint
val SecondaryDark      = Color(0xFF0F9E75)   // Darker teal

// ─── Status Colors ────────────────────────────────────────────────────────────
val Success            = Color(0xFF4CAF50)   // Green check icons
val SuccessContainer   = Color(0xFFE8F5E9)   // Green tint background
val SuccessLight       = Color(0xFFB8F0C8)   // Light green (appointment confirmed bg)

val Warning            = Color(0xFFFFC107)   // Amber/yellow (Pending badge)
val WarningContainer   = Color(0xFFFFF8E1)   // Yellow tint
val WarningText        = Color(0xFF8A6500)   // Text on warning badges

val Error              = Color(0xFFE53935)   // Red (cancel button, danger zone)
val ErrorContainer     = Color(0xFFFFEBEE)   // Red tint background
val ErrorDark          = Color(0xFFC62828)   // Darker red (Yes, Cancel btn)

// ─── Neutrals ────────────────────────────────────────────────────────────────
val White              = Color(0xFFFFFFFF)
val Background         = Color(0xFFF5F6FA)   // App background (light grey)
val Surface            = Color(0xFFFFFFFF)   // Card surface
val SurfaceVariant     = Color(0xFFF0F2F8)   // Input field background

val TextPrimary        = Color(0xFF1A1D2E)   // Near-black main text
val TextSecondary      = Color(0xFF6B7280)   // Muted/subtext grey
val TextTertiary       = Color(0xFF9CA3AF)   // Placeholder / hint text
val TextOnPrimary      = Color(0xFFFFFFFF)   // White text on blue
val TextLink           = Color(0xFF2D4ED8)   // Hyperlinks (Sign In, Forgot Password)

val BorderLight        = Color(0xFFE5E7EB)   // Card borders, dividers
val BorderFocus        = Color(0xFF2D4ED8)   // Focused input border

// ─── Badge / Pill Colors ──────────────────────────────────────────────────────
val BadgeReady         = Color(0xFFE6EDFF)   // Ready badge background (blue tint)
val BadgeReadyText     = Color(0xFF2D4ED8)   // Ready badge text
val BadgePending       = Color(0xFFFFF3CD)   // Pending badge background
val BadgePendingText   = Color(0xFF856404)   // Pending badge text
val BadgeUpcoming      = Color(0xFFD1FAE5)   // Upcoming badge background
val BadgeUpcomingText  = Color(0xFF065F46)   // Upcoming badge text
val BadgeUrgent        = Color(0xFFFFE4E6)   // Urgent badge background
val BadgeUrgentText    = Color(0xFFBE123C)   // Urgent badge text

// ─── Quick Action Icon Backgrounds ───────────────────────────────────────────
val IconBgBlue         = Color(0xFFEBEEFD)   // Lab Reports, Appointments icon bg
val IconBgPurple       = Color(0xFFF3EFFE)   // Chat icon bg
val IconBgOrange       = Color(0xFFFFF0E4)   // Profile icon bg
val IconBgTeal         = Color(0xFFE0F5EF)   // Secondary icon bg

// ─── Navigation ──────────────────────────────────────────────────────────────
val NavActive          = Color(0xFF2D4ED8)   // Active tab icon & label
val NavInactive        = Color(0xFF9CA3AF)   // Inactive tab icon & label

// ─── Call Screen ─────────────────────────────────────────────────────────────
val CallBackground     = Color(0xFF1A1D2E)   // Dark bg for call screen
val CallEndButton      = Color(0xFFE53935)   // Red end-call button

// ─── Dark Theme Counterparts ──────────────────────────────────────────────────
val PrimaryDarkTheme   = Color(0xFF6B8EF7)
val BackgroundDark     = Color(0xFF12131A)
val SurfaceDark        = Color(0xFF1E2030)
val SurfaceVariantDark = Color(0xFF2A2D42)
val TextPrimaryDark    = Color(0xFFF0F1F8)
val TextSecondaryDark  = Color(0xFF9AA3C2)
val BorderDark         = Color(0xFF2E3150)